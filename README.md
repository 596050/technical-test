1. In directory with package.json run npm install
2. Once the installation is complete, use "npm run server" and "npm run start"
3. Browser should show frontend automatically, otherwise navigate to http://localhost:3000/
4. To run tests use "npm run test:e2e"
